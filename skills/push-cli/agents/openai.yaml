interface:
  display_name: "Sub2API Push CLI"
  short_description: "Require Apple Containers on macOS, then push"
  default_prompt: "Use $push-cli to align the environment before the dirty-worktree gate, require Apple Containers on macOS with no Docker fallback, require Docker inside running WSL2 Debian or Ubuntu on Windows without host fallback, run strict local checks, and push the current branch safely."
