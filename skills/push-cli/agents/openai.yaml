interface:
  display_name: "Sub2API Push CLI"
  short_description: "Validate with Apple Containers or Docker, then push"
  default_prompt: "Use $push-cli to run strict local checks, validate the preferred host container runtime, and push the current branch safely."
